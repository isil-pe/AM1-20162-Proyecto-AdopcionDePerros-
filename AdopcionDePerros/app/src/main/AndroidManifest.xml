<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dvasq.adopciondeperros">

    <application
        android:name=".PerroApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InicioActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".PublicarActivity" />
        <activity android:name=".ListaPerroActivity" />
        <activity android:name=".PerroDetalleActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".MisPerrosActivity" />
        <activity android:name=".EditarPerroActivity"></activity>
    </application>

</manifest>